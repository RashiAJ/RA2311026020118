# RA2311026020118

Campus notification platform — logging middleware, Stage 1 priority inbox (`notification_app_be`), design doc, and frontend placeholders.

See `notification_app_be/README.md` for register / auth / `npm run stage1`.
