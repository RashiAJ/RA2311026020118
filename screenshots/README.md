# Screenshots (Stage 1)

Add screenshots here that show the **priority notifications** output from Stage 1, for example:

1. Run `npm run stage1` from `notification_app_be` with `NOTIFICATION_API_TOKEN` set.
2. Open `notification_app_be/output/stage1-priority-notifications.txt` or tail `notification_app_be/logs/stage1-priority-inbox.log`.
3. Capture the window showing the ordered top-N list and save as `stage1-priority-output.png` (or similar) in this folder.

Committed screenshots satisfy the evaluation requirement for visual proof of output.
